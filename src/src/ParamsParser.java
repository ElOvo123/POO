import java.io.IOException;
// ParamsParser.java - Handles parameter parsing
public class ParamsParser {
    public static Params fromFile(String filename) throws IOException {
        // Parse input file format as specified
    }
    
    public static Params randomParams(String[] args) {
        // Generate random obstacles and cost zones based on counts provided
    }
}